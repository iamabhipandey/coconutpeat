import { ZoomMode } from './zoom-mode';
import { NgxImageZoomService } from '../ngx-image-zoom.service';
export declare class HoverFreezeZoomMode implements ZoomMode {
    private zoomService;
    private zoomFrozen;
    constructor(zoomService: NgxImageZoomService);
    onClick(event: MouseEvent): void;
    onMouseEnter(event: MouseEvent): void;
    onMouseLeave(): void;
    onMouseMove(event: MouseEvent): void;
    onMouseWheel(): boolean;
}
